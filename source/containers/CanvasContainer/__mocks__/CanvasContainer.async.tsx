/* eslint-disable react/jsx-filename-extension */
import React from 'react';

const CanvasContainer = props => (
    <div data-mock='CanvasContainer'>
        {props.children}
    </div>
);

export default CanvasContainer;
