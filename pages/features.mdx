import Image from 'next/image';
import { Container } from '../src/components/Container/Container';
import { Layout } from '../src/components/Layout/Layout';
import { Cell } from '../src/components/Layout/Cell';

# ImgReview Features

ImgReview has number of cool features, let me show you some of them.

<Layout>
  <Cell span={6}>
    <h3>Add multiple images</h3>
    You can add as many images as you want. So it's never been that easy to compare
    several images side by side.
  </Cell>
  <Cell span={6}>
    <h3>Easy to add arrows and shapes</h3>
    The main feature that caused me to develop this app. You can add different shapes
    and (of course) arrows to make your point.
  </Cell>
  <Cell span={6}>
    <Image
      alt="ImgReview - Add multiple images"
      loader={({ src }) => src}
      unoptimized
      width={900}
      height={600}
      src="/images/features-multiple-images.gif"
    />
  </Cell>
  <Cell span={6}>
    <Image
      alt="ImgReview - Easy to add arrows and shapes"
      loader={({ src }) => src}
      unoptimized
      width={900}
      height={600}
      src="/images/features-add-shape-arrow.gif"
    />
  </Cell>
  <Cell span={6}>
    <h3>Copy all</h3>
    You can copy your whole work to the clipboard in one click and then paste it
    in any app of your choosing: gmail, slack, docs, you name it.
  </Cell>
  <Cell span={6}>
    <h3>Sketchify</h3>
    Add "human" touch to your work. Sketchify will add a bit of imperfection to your
    shapes, making it unique.
  </Cell>
  <Cell span={6}>
    <Image
      alt="ImgReview - Copy all"
      loader={({ src }) => src}
      unoptimized
      width={900}
      height={600}
      src="/images/features-copy-all.gif"
    />
  </Cell>
  <Cell span={6}>
    <Image
      alt="ImgReview - Sketchify"
      loader={({ src }) => src}
      unoptimized
      width={900}
      height={600}
      src="/images/features-sketchify.gif"
    />
  </Cell>
  <Cell span={6}>
    <h3>Add emotions</h3>
    You can easily add emojis. Also if you make them bigger, they will really stand
    out.
  </Cell>
  <Cell span={6}>&nbsp;</Cell>
  <Cell span={6}>
    <Image
      alt="ImgReview - Add emotions"
      loader={({ src }) => src}
      unoptimized
      width={900}
      height={600}
      src="/images/features-emoji.gif"
    />
  </Cell>
  <Cell span={6}>&nbsp;</Cell>
</Layout>

export default ({ children }) => <Container>{children}</Container>;
