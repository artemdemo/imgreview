import styled from 'styled-components';
import rectRoughImg from './img/rect-rough.svg';

const IconRectRough = styled.span`
  background-image: url(${rectRoughImg});
  width: 14px;
  height: 16px;
  background-repeat: no-repeat;
  display: block;
`;

export default IconRectRough;
